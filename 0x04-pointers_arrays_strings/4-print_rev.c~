/* 4. Print string in reverse */

#include "holberton.h"

/**
 * print_rev - print string in reverse
 * @s: string to reverse
 *
 * Return: none
 */
void print_rev(char *s)
{
	int length = 0, i;

	while(*s)
	{
		s++;
		length++;
	}

	for (i = length; i >= 0; i--)
		_putchar(*(s + i));
}
