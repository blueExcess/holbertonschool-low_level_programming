/* 2. Return length of a string */
#include "holberton.h"

/**
 * _strlen_recursion - find length of string
 * @s: string to measure
 *
 * Return: int
 */
int _strlen_recursion(char *s)
{
	int count;

	if (*s)
	{
		count++;
		s++;
		_strlen_recursion(*s);
	}
}
