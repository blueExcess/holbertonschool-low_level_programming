/* 6. print 0 - 9 only with 'putchar()' */

#include <stdio.h>

/**
 * main - print 0-9
 *
 * Return: 0 on success
 */
int main(void)
{
	int x;

	for (x = '0'; x <= '9'; x++)
		putchar(x);
	putchar('\n');
	return (0);
}
