/* 6. compute absolute value of int */

#include "holberton.h"

/**
 * _abs - return absolute value
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
	{
		n = !n;
		return (n);
	}
}
