#include "sort.h"

/**
 * radix_sort - sort via the radix method.
 * @array: array to be sorted.
 * @size: size of array.
 *
 * Return: VOID
 */
void radix_sort(int *array, size_t size)
{

	if (array == NULL)
		return;

}
