#include "sort.h"

/**
 * shell_sort - sort via shell method.
 * @array: array to sort
 * @size: size of array
 *
 * Return: VOID
 */
void shell_sort(int *array, size_t size)
{

	if (array == NULL)
		return;
}
