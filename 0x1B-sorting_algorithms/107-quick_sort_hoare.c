#include "sort.h"

/**
 * quick_sort_hoare - sort via second quicksort method.
 * @array: array to sort.
 * @size: size of array.
 *
 * Return: VOID
 */
void quick_sort_hoare(int *array, size_t size)
{
	if (array == NULL)
		return;
}
