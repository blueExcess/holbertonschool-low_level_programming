#include "sort.h"

/**
 * heap_sort - sort via heap method.
 * @array: array to be sorted.
 * @size: size of array.
 *
 * Return: VOID
 */
void heap_sort(int *array, size_t size)
{
	if (array == NULL)
		return

}
