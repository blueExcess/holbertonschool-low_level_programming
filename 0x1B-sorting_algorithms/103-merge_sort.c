#include "sort.h"

/**
 * merge_sort - sort via merge method.
 * @array: array to be sorted.
 * @size: size of array.
 *
 * Return: VOID
 */
void merge_sort(int *array, size_t size)
{
	if (array == NULL)
		return;

}
