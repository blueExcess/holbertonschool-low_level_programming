#include "sort.h"

/**
 * bitonic_sort - sort via bitonic method.
 * @array: array to be sorted.
 * @size: size of array.
 *
 * Return: VOID
 */
void bitonic_sort(int *array, size_t size)
{
	if (array == NULL)
		return;
}
