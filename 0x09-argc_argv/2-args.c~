/* 2. print all arguments received */
#include "holberton.h"
#include <stdio.h>

/**
 * main - print all args passed
 * @argc: number of args
 * @argv: args
 *
 * Return: 0 on success
 */
int main(int argc, char **argv)
{
	while (argc--)
		printf("%s\n", *argv++);
	return (0);
}
